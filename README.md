Generates an html webpage with information from a tuple hardcoded into the source code.

```
python main.py
```

The webpage should be `index.html` located in the same directory where the program was run from.
